/// Represents the chess color assigned to a player in a match.
enum Color {
  /// The white pieces.
  white,

  /// The black pieces.
  black,

  /// No color assigned (e.g., in case of a bye).
  noColor,
}
